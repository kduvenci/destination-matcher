<div class="wrapper">
  <div class="container">
    <h3 class="text-center">Our recommended destinations for you</h3>
    <div class="row">

      <% if @result_cities == [] %>
        <h3>Sorry, there are no destinations within your budget!</h3>
      <% else %>
       <% @result_cities.each do |city, flight_ids| %>
         <% country = city.country %>
          <div class="col-xs-12.offset-xs-2 col-sm-6 col-md-6 col-lg-4">
            <%= link_to city_path(city, flight_ids: flight_ids.join("-")) do %>
              <div class="card">
                <%= cl_image_tag city.photo, height: 300, width: 370, crop: :fill %>
                <div class="card-overlay"></div>
                <span class="title"><h3><%= city.name %>, <%= country.name %></h3></span>
                <span class="info">
                <h4>Language: <%= country.language %></h4>
                <h4>English Level: <span class="yellow"><%= '★' * country.english_level %></span></h4>
                <% if city.overrall_risk == 'low' %>
                  <h4>Overall Risk: <span class="blue"><%= city.overrall_risk.capitalize %></h4></span>
                  <% elsif city.overrall_risk == 'medium' %>
                  <h4>Overall Risk: <span class="yellow"><%= city.overrall_risk.capitalize %></h4></span>
                  <% else %>
                  <h4>Overall Risk: <span class="red"><%= city.overrall_risk.capitalize %></h4></span>
                <% end %>
                <h4>Currency: <%= country.currency %></h4>
                </span>
              </div>
            <% end %>
          </div>
        <% end %>
        <% end %>
      </div>
   </div>
</div>

<%= cities.overrall_risk.capitalize %></h4>



